{
  "name": "called-out",
  "version": "0.1.0",
  "private": true,
  "description": "Called Out — live & uploaded debate scoring with fact-check + fallacy detection",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
